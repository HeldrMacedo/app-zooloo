import { colors } from '@/assets/styles/colors';

/**
 * Cores das barras do sistema (status bar + navigation bar / home indicator).
 * Fundo azul + ícones claros (light) para legibilidade.
 */
export const SYSTEM_BAR_COLOR = colors.brand.system;
export const SYSTEM_BAR_STYLE = 'light' as const; // ícones brancos
